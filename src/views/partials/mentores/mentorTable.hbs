{{#if mentores}}
  <table class="table table-bordered table-hover table-dark">
    <thead>
      <tr>
        <th>N°</th>
        <th>Dni</th>
        <th>Nivel</th>
        <th>Id Mentor Superior</th>
      </tr>
    </thead>
    <tbody>
      {{#each mentores}}
        <tr>
          <td>{{@index}}</td>
          <td><strong>{{dni}}</strong></td>
          <td>{{nivel}}</td>
          <td>{{idMentorSuperior}}</td>
          <td>
            <a href="/mentores/{{_id}}/delete" class="btn btn-danger">
              Delete
            </a>
            <a href="/mentores/{{_id}}/edit" class="btn btn-info">
              Edit
            </a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="h-100 d-flex align-items-center justify-content-center">
    <div class="text-center text-white">
      <h1>No se encontraron Mentores</h1>
      <img src="/icons/empty.svg" class="w-75" />
    </div>

  </div>
{{/if}}